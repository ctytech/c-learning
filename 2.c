#include <stdio.h>
#include <stdlib.h>
int main (void)
{
//取消对应程序的多行注释以使用
	
//计算数组中每个数的总和
/*	int x, right, sum = 0;

	scanf ("%i", &x);
	while ( x != 0 ) {

	right = x % 10;
	sum = sum + right;
	x = x / 10;

	
	}

	printf ("%i\n", sum);
*/
//阶乘计算器
/*	int ntime, number, n = 1;

	scanf("%i", &number);
	for ( ntime = 1; ntime <= number; ntime++)
		n = n * ntime;

	printf("%i\n", n);

*/

//平方计算器
/*	int square, number, x;

	printf ("Number list \n");
	printf ("=========== \n");

	for ( number = 1 ; number <= 10; number++) {

	
		square = number * number;
		printf ("%i\n", square);
	}
*/


//计算三角的形的每个点（教程项目，见书）（可让用户输入）
/*	int time;


	printf("What times do you want ? ");
	scanf("%i", &time);
	for ( int counter = 1, number, cout; counter <= time; ++counter) {

		printf ("What ");
		scanf ("%i", &number);

	

	cout = 0;

	for ( int num = 1; num <= number; num++ )
		cout += num;

	printf("%i\n", cout);

	}
*/
return 0;
}
